{% extends 'base.html.twig' %}

{% block title %}Product index{% endblock %}

{% block body %}
    <h1>Liste des Produits</h1>

    {% include 'layout/_flash_message.html.twig' %}

    <a class="btn btn-outline-primary" href="{{ path('app_product_new') }}">Ajouter un produit</a>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Description</th>
                <th>Prix</th>
                <th>Taille XS</th>
                <th>Taille S</th>
                <th>Taille M</th>
                <th>Taille L</th>
                <th>Taille XL</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for product in products %}
            <tr>
                <td>{{ product.id }}</td>
                <td>{{ product.nom }}</td>
                <td>{{ product.description }}</td>
                <td>{{ product.price }}</td>
                <td>{{ product.stockXS }}</td>
                <td>{{ product.stockS }}</td>
                <td>{{ product.stockM }}</td>
                <td>{{ product.stockL }}</td>
                <td>{{ product.stockXL }}</td>
                <td>
                    <a class="btn btn-outline-primary" href="{{ path('app_product_show', {'id': product.id}) }}">Voir l'article</a>
                    <a class="btn btn-outline-info" href="{{ path('app_product_edit', {'id': product.id}) }}">Modifier</a>
                    {{ include('product/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="10">Pas de produit enregistré</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

  
{% endblock %}
