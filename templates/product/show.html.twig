{% extends 'base.html.twig' %}

{% block title %}Product{% endblock %}

{% block body %}
    <h1>Fiche produit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ product.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ product.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ product.price }}</td>
            </tr>
            <tr>
                <th>Stock_XS</th>
                <td>{{ product.stockXS }}</td>
            </tr>
            <tr>
                <th>Stock_S</th>
                <td>{{ product.stockS }}</td>
            </tr>
            <tr>
                <th>Stock_M</th>
                <td>{{ product.stockM }}</td>
            </tr>
            <tr>
                <th>Stock_L</th>
                <td>{{ product.stockL }}</td>
            </tr>
            <tr>
                <th>Stock_XL</th>
                <td>{{ product.stockXL }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-primary" href="{{ path('app_product_index') }}">Revenir à la liste</a>

    <a class="btn btn-outline-primary" href="{{ path('app_product_edit', {'id': product.id}) }}">Modifier</a>

    {{ include('product/_delete_form.html.twig') }}
{% endblock %}
